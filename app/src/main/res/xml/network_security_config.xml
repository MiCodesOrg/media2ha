<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Artwork is fetched from local media servers that may only speak plain HTTP. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
